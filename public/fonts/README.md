The website uses the local Mersad variable font:

- Mersad-26958361069.ttf (weight axis: 100-900)

The individual Mersad OTF weight files are retained as source assets. The active font family, typography hierarchy, colors, radii, and reusable UI styles are configured in src/theme.css.
